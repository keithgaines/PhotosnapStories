import { ListrTask } from 'listr2';
import yargs from 'yargs';
import { AuthGeneratorCtx } from './authTasks';
export declare const standardAuthBuilder: (yargs: yargs.Argv<{}>) => yargs.Argv<{
    force: boolean;
} & {
    verbose: boolean;
}>;
interface Args {
    basedir: string;
    forceArg: boolean;
    provider: string;
    authDecoderImport?: string;
    webAuthn?: boolean;
    webPackages?: string[];
    apiPackages?: string[];
    extraTask?: ListrTask<AuthGeneratorCtx>;
    notes?: string[];
    verbose?: boolean;
}
/**
 *  basedir assumes that you must have a templates folder in that directory.
 *  See folder structure of auth providers in packages/auth-providers/<provider>/setup/src
 */
export declare const standardAuthHandler: ({ basedir, forceArg, provider, authDecoderImport, webAuthn, webPackages, apiPackages, extraTask, notes, verbose, }: Args) => Promise<void>;
export {};
//# sourceMappingURL=setupHelpers.d.ts.map