{
  "framework": null,
  "installCommand": "corepack enable && corepack prepare yarn@3.5.0 --activate && yarn install --immutable",
  "buildCommand": "yarn rw build && cp web/src/*.css web/dist/",
  "outputDirectory": "web/dist",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/200.html"
    }
  ]
}
